var addtask = require('./AddTask').addTask
var updatetask = require('./UpdateTask').updateTask
var deletetask = require('./DeleteTask').removeTask

module.exports = {
  addtask,
  updatetask,
  deletetask
}